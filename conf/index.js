/**
 * 配置
 * @type {{webhost: string, db: {host: string, name: string}}}
 */
const config = {
    port: '8666',
    webhost: 'http://dwz.loobool.com/',
    db: {
        host: '127.0.0.1',
        name: 'dwz'
    }
};

module.exports = config;

