/**
 * 配置
 * @type {{webhost: string, db: {host: string, name: string}}}
 */
const config = {
    webhost: 'http://127.0.0.1:3000/',
    db: {
        host: '127.0.0.1',
        name: 'dwz'
    }
};

module.exports = config;

